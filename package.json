{
  "name": "lostfoundtracker",
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm run build && cd ../backend && npm install",
    "start": "cd backend && npm start",
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm start\""
  },
  "dependencies": {
    "concurrently": "^7.6.0"
  }
}